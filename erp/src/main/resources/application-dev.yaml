spring:
  datasource:
    url: jdbc:mysql://localhost:3306/erp?serverTimezone=Asia/Seoul
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 비밀번호
  jpa:
    hibernate:
      ddl-auto: validate   # MySQL 전환 후엔 validate로
    database-platform: org.hibernate.dialect.MySQL8Dialect